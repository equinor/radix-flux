apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: csi-secrets-store-provider-azure
  namespace: kube-system
spec:
  chart:
    spec:
      version: v1.2.0 # https://github.com/Azure/secrets-store-csi-driver-provider-azure/releases
  values:
    secrets-store-csi-driver:
      syncSecret:
        enabled: true