apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-acr-cleanup
  namespace: default
spec:
  values:
    deleteUntagged: true
    retainLatestUntagged: 0
    image:
      tag: release-75e229f3-1641542746 # {"$imagepolicy": "flux-system:radix-acr-cleanup:tag"}
    resources:
      limits:
        cpu: "2"
        memory: 150Mi
      requests:
        cpu: 50m
        memory: 150Mi
