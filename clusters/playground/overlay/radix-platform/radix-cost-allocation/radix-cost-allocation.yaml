apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: radix-cost-allocation
  namespace: flux-system
spec:
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./components/radix-platformv2/radix-cost-allocation
  prune: false
  postBuild:
    substitute:
      RADIX_ENVIRONMENT: ${RADIX_ENVIRONMENT} # Set in postBuild playground
      radix_acr_repo_url: ${radix_acr_repo_url} # Set in postBuild playground
      radix_cost_allocation_helm_chart_branch: release
      radix_cost_allocation_image_tag_prefix: release
    substituteFrom:
      - kind: ConfigMap
        name: radix-flux-config
  patches:
    - patch: |-
        apiVersion: helm.toolkit.fluxcd.io/v2beta1
        kind: HelmRelease
        metadata:
          name: radix-cost-allocation
          namespace: radix-cost-allocation
        spec:
          values:
            image:
              tag:  ${RADIX_COST_ALLOCATION_TAG} # Set in postBuild playground
            resources:
              limits:
                cpu: "2"
                memory: 150Mi
              requests:
                cpu: 50m
                memory: 150Mi
      target:
        kind: HelmRelease
        name: radix-cost-allocation
        namespace: radix-cost-allocation
