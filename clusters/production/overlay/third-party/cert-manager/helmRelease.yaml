apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  chart:
    spec:
      version: 1.7.1 # https://artifacthub.io/packages/helm/cert-manager/cert-manager
  values:
    resources:
      limits:
        cpu: 200m
        memory: 1Gi
      requests:
        cpu: 150m
        memory: 800Mi
