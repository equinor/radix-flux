apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: velero
  namespace: velero
spec:
  chart:
    spec:
      version: 2.27.0 # https://artifacthub.io/packages/helm/vmware-tanzu/velero
  values:
    resources:
      limits:
        cpu: "6"
        memory: 4096Mi
      requests:
        cpu: "2"
        memory: 2048Mi
