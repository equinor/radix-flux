apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: grafana
  namespace: monitor
spec:
  chart:
    spec:
      version: 6.58.5 # https://artifacthub.io/packages/helm/grafana/grafana
  values:
    env:
      GF_DATABASE_HOST: s940-radix-grafana-platform-prod.mysql.database.azure.com
      GF_DATABASE_NAME: grafana
      GF_DATABASE_USER: grafana
