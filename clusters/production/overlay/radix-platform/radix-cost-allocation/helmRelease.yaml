apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-cost-allocation
  namespace: radix-cost-allocation
spec:
  values:
    image:
      tag: master-6f810e44-1634278063 # {"$imagepolicy": "flux-system:radix-cost-allocation:tag"}
    resources:
      limits:
        cpu: "2"
        memory: 300Mi
      requests:
        cpu: 100m
        memory: 300Mi
