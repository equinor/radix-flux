apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-cost-allocation
  namespace: radix-cost-allocation
spec:
  values:
    image:
      tag: release-6cbfb9f6-1643362566 # {"$imagepolicy": "flux-system:radix-cost-allocation:tag"}
    resources:
      limits:
        cpu: "2"
        memory: 300Mi
      requests:
        cpu: 100m
        memory: 300Mi
