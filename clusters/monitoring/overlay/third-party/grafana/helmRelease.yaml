apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: grafana
  namespace: default
spec:
  chart:
    spec:
      version: 6.20.3 # https://artifacthub.io/packages/helm/grafana/grafana
  values:
    resources:
      requests:
        cpu: "0.2"
        memory: "200Mi"
    envFromSecret: null
