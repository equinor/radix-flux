apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: HelmChart
metadata:
  name: radix-acr-cleanup
  namespace: default
spec:
  chart: ./charts/radix-acr-cleanup
  sourceRef:
    name: radix-acr-cleanup
    kind: GitRepository
  interval: 10m
  valuesFiles:
    - ./values.yaml