apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: default
spec:
  chart:
    spec:
      version: 6.7.5 # https://artifacthub.io/packages/helm/bitnami/external-dns
