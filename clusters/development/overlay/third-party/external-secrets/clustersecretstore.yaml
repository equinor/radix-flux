apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: radix-keyvault
  namespace: external-secrets
spec:
  provider:
    azurekv:
      vaultUrl: https://radix-keyv-${RADIX_ZONE}.vault.azure.net/
