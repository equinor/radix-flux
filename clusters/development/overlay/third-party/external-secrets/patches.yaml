apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-secrets-operator
  namespace: flux-system
spec:
  patches:
    - target:
        kind: HelmRelease
        name: external-secrets-operator
        namespace: external-secrets
      patch: |-
        - op: replace
          path: /spec/chart/spec/version
          value: ${EXTERNAL_SECRETS} # Set in postBuild development
    - target:
        kind: HelmRelease
        name: external-secrets-operator
        namespace: external-secrets
      patch: |-
        - op: add
          path: /spec/values/resources
          value: 
            limits:
              memory: 64Mi
            requests:
              memory: 64Mi
              cpu: 5m
        - op: add
          path: /spec/values/certController/resources
          value: 
            limits:
              memory: 64Mi
            requests:
              memory: 64Mi
              cpu: 1m
        - op: add
          path: /spec/values/webhook/resources
          value: 
            limits:
              memory: 64Mi
            requests:
              memory: 64Mi
              cpu: 1m
#        apiVersion: helm.toolkit.fluxcd.io/v2
#        kind: HelmRelease
#        metadata:
#          name: external-secrets-operator
#          namespace: external-secrets
#        spec:
#          chart:
#            spec:
#              version: ${EXTERNAL_SECRETS} # Set in postBuild development
