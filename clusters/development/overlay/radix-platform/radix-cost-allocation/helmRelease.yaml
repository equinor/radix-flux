apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-cost-allocation
  namespace: radix-cost-allocation
spec:
  values:
    image:
      tag: ${RADIX_COST_ALLOCATION_TAG} # Set in postBuild development
    resources:
      limits:
        cpu: "2"
        memory: 100Mi
      requests:
        cpu: 100m
        memory: 100Mi
