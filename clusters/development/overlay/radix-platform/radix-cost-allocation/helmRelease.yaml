apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-cost-allocation
  namespace: radix-cost-allocation
spec:
  values:
    image:
      tag: master-e8b3d2cc-1644218856 # {"$imagepolicy": "flux-system:radix-cost-allocation:tag"}
    resources:
      limits:
        cpu: "2"
        memory: 100Mi
      requests:
        cpu: 100m
        memory: 100Mi
