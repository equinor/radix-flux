apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-acr-cleanup
  namespace: default
spec:
  values:
    deleteUntagged: false
    image:
      tag: master-b48e7e6b-1645516532 # {"$imagepolicy": "flux-system:radix-acr-cleanup:tag"}
    resources:
      limits:
        cpu: "2"
        memory: 150Mi
      requests:
        cpu: 50m
        memory: 150Mi
