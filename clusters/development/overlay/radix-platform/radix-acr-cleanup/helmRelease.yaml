apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-acr-cleanup
  namespace: default
spec:
  values:
    deleteUntagged: false
    image:
      tag: master-27f8d59b-1643798562 # {"$imagepolicy": "flux-system:radix-acr-cleanup:tag"}
    resources:
      limits:
        cpu: "2"
        memory: 150Mi
      requests:
        cpu: 50m
        memory: 150Mi
