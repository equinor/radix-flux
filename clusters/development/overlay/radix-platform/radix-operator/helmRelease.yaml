apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-operator
  namespace: default
spec:
  values:
    image:
      tag: master-fe7a502a-1637051177 # {"$imagepolicy": "flux-system:radix-operator:tag"}
    activeClusterName: ${ACTIVE_CLUSTER} # Set in postBuild development
    resources:
      limits:
        cpu: "2"
        memory: 1000Mi
      requests:
        cpu: 100m
        memory: 1000Mi
    subscriptionId: 16ede44b-1f74-40a5-b428-46cca9a5741b
