apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-operator
  namespace: default
spec:
  values:
    activeClusterName: weekly-41
    resources:
      limits:
        memory: 1000Mi
      requests:
        memory: 1000Mi
