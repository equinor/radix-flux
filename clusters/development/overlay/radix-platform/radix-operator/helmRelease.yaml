apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-operator
  namespace: default
spec:
  values:
    image:
      tag: master-d8502148-1634219192 # {"$imagepolicy": "flux-system:radix-operator:tag"}
    activeClusterName: weekly-41
    resources:
      limits:
        memory: 1000Mi
      requests:
        memory: 1000Mi
