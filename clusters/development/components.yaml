apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: components
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: flux-system
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./clusters/development/overlay
  prune: true
  validation: client
  # healthChecks:
  #   - apiVersion: helm.toolkit.fluxcd.io/v1beta1
  #     kind: HelmRelease
  #     name: podinfo
  #     namespace: podinfo
