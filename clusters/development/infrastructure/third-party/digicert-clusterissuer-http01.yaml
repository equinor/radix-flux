apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: digicert-clusterissuer-http01
  namespace: flux-system
spec:
  dependsOn:
    - name: cert-manager
    - name: digicert-externalaccount
  interval: 5m
  path: "./components/third-party/cert-manager-issuers-v2/digicert/http01-ingress"
  postBuild:
    substituteFrom:
      - kind: Secret
        name: digicert-clusterissuer-flux-values
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  timeout: 2m
