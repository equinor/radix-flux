apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: digicert-clusterissuer-dns01
  namespace: flux-system
spec:
  dependsOn:
    - name: cert-manager
    - name: digicert-externalaccount
  interval: 5m
  path: "./components/third-party/cert-manager-issuers-v2/digicert/dns01"
  postBuild:
    substituteFrom:
      - kind: Secret
        name: digicert-clusterissuer-flux-values
      - kind: ConfigMap
        name: radix-flux-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  timeout: 2m
