apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: variables
spec:
  interval: 5m
  postBuild:
    substitute:
      test_var: "some_string"
    # substituteFrom:
    #   - kind: ConfigMap
    #     name: cluster-vars
    #   - kind: Secret
    #     name: cluster-secret-vars