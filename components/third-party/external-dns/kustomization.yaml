apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmRepo.yaml
- helmRelease.yaml
# - variables.yaml
spec:
  postBuild:
    substitute:
      cluster_env: "prod"
      cluster_region: "eu-central-1"