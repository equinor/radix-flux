apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: gateway-api
  namespace: flux-system
spec:
  interval: 1m
  url: oci://ghcr.io/kubernetes-sigs/gateway-api
  ref:
    tag: ${GATEWAY_API_VERSION} # Set in cluster postBuild.yaml
  secretRef:
    name: radix-external-registry-default-auth
