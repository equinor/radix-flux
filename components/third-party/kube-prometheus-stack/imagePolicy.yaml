apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: kube-prometheus-stack-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: kube-prometheus-stack-chart
  policy:
    semver:
      range: ">=0.0.0"
