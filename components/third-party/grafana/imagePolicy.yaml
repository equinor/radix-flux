apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: grafana-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: grafana-chart
  policy:
    semver:
      range: "<13.0.0"
