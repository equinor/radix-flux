apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: velero
  namespace: velero
resources:
- namespace.yaml
- helmRepo.yaml
- helmRelease.yaml
# patches:
#   - path: ${patch_path}
