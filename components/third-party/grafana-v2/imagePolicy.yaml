apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: grafana-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: grafana-chart
  policy:
    semver:
      range: ">=1.0.0"
