apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: digicert-http01
spec:
  acme:
    email: ${accountEmail}
    externalAccountBinding:
      keyID: ${accountKeyID}
      keySecretRef:
        key: secret
        name: digicert-account-hmac-key
    privateKeySecretRef:
      name: digicert-http01-private-key
    server: ${acmeServer}
    skipTLSVerify: false
    solvers:
    - http01:
        ingress:
          class: nginx
      selector: {}