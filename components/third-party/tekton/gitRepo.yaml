apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: tekton-pipelines
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/equinor/tekton-helm-chart
  ref:
    branch: main
