apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: tekton-operator-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: tekton-operator-chart
  policy:
    semver:
      range: ">=0.0.0-0"
