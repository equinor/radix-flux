apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: tekton-operator-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: tekton-operator-chart
  policy:
    semver:
      range: ">=1.0.0"
