apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: radix-cluster-cleanup-chart
  namespace: flux-system
spec:
  interval: 1m
  url: oci://ghcr.io/equinor/radix/charts/radix-cluster-cleanup
  ref:
    tag: ${RADIX_CLUSTER_CLEANUP_CHART_VERSION} # Set in cluster postBuild.yaml
