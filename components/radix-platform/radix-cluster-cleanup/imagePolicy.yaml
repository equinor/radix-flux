apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: radix-cluster-cleanup-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radix-cluster-cleanup-chart
  policy:
    semver:
      range: '>=0.0.0'
