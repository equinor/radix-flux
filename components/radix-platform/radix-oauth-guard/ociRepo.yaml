apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: radix-oauth-guard-chart
  namespace: flux-system
spec:
  interval: 1m
  url: oci://ghcr.io/equinor/radix/charts/radix-oauth-guard
  ref:
    tag: ${RADIX_OAUTH_GUARD_CHART_VERSION} # Set in cluster postBuild.yaml
