apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: radix-oauth-guard-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radix-oauth-guard-chart
  policy:
    semver:
      range: '>=0.0.0'
