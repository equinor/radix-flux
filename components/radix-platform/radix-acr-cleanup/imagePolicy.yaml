apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: radix-acr-cleanup
  namespace: flux-system
spec:
  filterTags:
    # pattern: ^master-[a-f0-9]+-(?P<ts>[0-9]+) # Set by flux patch
    extract: $ts
  imageRepositoryRef:
    name: radix-acr-cleanup
    namespace: default
  policy:
    numerical:
      order: asc
