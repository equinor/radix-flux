apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: radix-acr-cleanup-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radix-acr-cleanup-chart
  policy:
    semver:
      range: '>=0.0.0'
