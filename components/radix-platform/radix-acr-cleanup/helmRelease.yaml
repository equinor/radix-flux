apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radix-acr-cleanup
  namespace: default
  # annotations:
  #   # flux.weave.works/ignore: "true"
  #   fluxcd.io/automated: "true"
  #   filter.fluxcd.io/chart-image: glob:master-*
spec:
  interval: 5m
  install:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./charts/radix-acr-cleanup
      sourceRef:
        kind: GitRepository
        name: radix-acr-cleanup
      valuesFiles:
        - ./charts/radix-acr-cleanup/values.yaml
  values:
    registry: radixdev                                                # Set by flux patch
    # deleteUntagged: false                                           # Set by flux patch
    performDelete: true
    period: 1h
    cleanupStart: "0:00"
    cleanupEnd: "06:00"
    image:
      # For Flux "image.repository" means full path to the image (registry/repository)
      # The following 2 properties are mandatory for Flux image filtering
      # repository: radixdev.azurecr.io/radix-acr-cleanup             # Set by flux patch
      # tag: "master-bd3b024df51ecd6d4e75d7dc16d69955f2de0687"        # Set by flux patch
    metrics:
      enabled: true
    resources:
      limits:
        cpu: "2"
        memory: 150Mi
      requests:
        cpu: 50m
        memory: 150Mi
  valuesFrom:
  - kind: ConfigMap
    name: radix-platform-config
    valuesKey: platform
