apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: radix-acr-cleanup-chart
  namespace: flux-system
spec:
  interval: 1m
  url: oci://ghcr.io/equinor/radix/charts/radix-acr-cleanup
  ref:
    tag: ${RADIX_ACR_CLEANUP_CHART_VERSION} # Set in cluster postBuild.yaml
