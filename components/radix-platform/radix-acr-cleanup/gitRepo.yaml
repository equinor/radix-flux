apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: radix-acr-cleanup
  namespace: default
spec:
  interval: 1m
  url: https://github.com/equinor/radix-acr-cleanup
  ref:
    # branch: master        # Set by patch