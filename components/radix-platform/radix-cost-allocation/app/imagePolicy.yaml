apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: radix-cost-allocation-chart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radix-cost-allocation-chart
  policy:
    semver:
      range: '>=0.0.0'
