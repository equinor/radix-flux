apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageRepository
metadata:
  name: radix-operator
  namespace: default
spec:
  # image: xx                 # Set by flux patch
  interval: 1m0s
  secretRef:
    name: radix-docker
