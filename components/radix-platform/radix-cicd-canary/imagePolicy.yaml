apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: radix-cicd-canary-chart
  namespace: flux-system      
spec:
  imageRepositoryRef:
    name: radix-cicd-canary-chart
  policy:
    semver:
      range: ${RADIX_CICD_CANARY_CHART_POLICY_SEMVER_RANGE} # ${RADIX_CICD_CANARY_CHART_POLICY_SEMVER_RANGE:=">=0.0.1"}
