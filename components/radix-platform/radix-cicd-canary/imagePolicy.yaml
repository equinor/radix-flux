apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: radix-cicd-canary-chart
  namespace: flux-system      
spec:
  imageRepositoryRef:
    name: radix-cicd-canary-chart
  policy:
    semver:
      range: '>=0.0.0'
