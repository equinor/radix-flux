apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: radix-velero-plugin
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radix-velero-plugin
  policy:
    semver:
      range: '>=0.0.0'
