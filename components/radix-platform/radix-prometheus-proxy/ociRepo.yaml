apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: radix-prometheus-proxy-chart
  namespace: flux-system
spec:
  interval: 1m
  url: oci://ghcr.io/equinor/radix/charts/radix-prometheus-proxy
  ref:
    tag: ${RADIX_PROMETHEUS_PROXY_CHART_VERSION} # Set in cluster postBuild.yaml
