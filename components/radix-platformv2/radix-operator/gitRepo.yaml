apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: radix-operator
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/equinor/radix-operator
  ref:
    branch: ${radix_operator_helm_chart_branch} # Defined in flux patch
