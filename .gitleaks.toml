# Gitleaks configuration file
# Documentation: https://github.com/gitleaks/gitleaks

title = "gitleaks config"

# Exclude specific paths from scanning
[allowlist]
paths = [
    '''clusters/development/postBuild\.yaml'''
]

# Optional: Add description for why this path is excluded
description = "Excluding development postBuild configuration from secret scanning"
