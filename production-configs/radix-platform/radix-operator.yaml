---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: radix-operator
  namespace: default
  annotations:
    # flux.weave.works/ignore: "true"
    flux.weave.works/automated: true
    flux.weave.works/tag.chart-image: glob:release-*
spec:
  releaseName: "radix-operator"
  chart:
    git: "git@github.com:equinor/radix-flux"
    ref: "release"
    path: "charts/radix-operator"
  values:
    image:
      # For Flux "image.repository" means full path to the image (registry/repository)
      # The following 2 properties are mandatory for Flux image filtering
      repository: radixprod.azurecr.io/radix-operator
      tag: "release-15574c9e37f4f976583a2f40f7b7e17d2de776da"
  valuesFrom:
  - configMapKeyRef:
      name: "radix-platform-config"
      key: "platform"
      optional: false
